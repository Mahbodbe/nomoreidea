<div align="center">

# ⚡ مهبد بمانی‌چم

**مهندسی برق — گرایش کنترل · ماینور الکترونیک**
**دانشگاه صنعتی امیرکبیر (پلی‌تکنیک تهران)**

[![Portfolio](https://img.shields.io/badge/🌐_نسخه_زنده-mahbodbe.github.io%2Fnomoreidea-3b82f6?style=for-the-badge)](https://mahbodbe.github.io/nomoreidea/)
[![GitHub](https://img.shields.io/badge/GitHub-@Mahbodbe-181717?style=for-the-badge&logo=github&logoColor=white)](https://github.com/Mahbodbe)
[![Telegram](https://img.shields.io/badge/تلگرام-@Mb__Mb84-26A5E4?style=for-the-badge&logo=telegram&logoColor=white)](https://t.me/Mb_Mb84)
[![LinkedIn](https://img.shields.io/badge/LinkedIn-mahbod--bemanicham-0A66C2?style=for-the-badge&logo=linkedin&logoColor=white)](https://www.linkedin.com/in/mahbod-bemanicham-048b252ab)

[🇬🇧 **English Version**](README.md) | 🇮🇷 **نسخه فارسی (فعلی)**

*این مخزن، سورس‌کد وبسایت پورتفولیوی شخصی من است؛ یک ایندکس مهندسیِ دست‌ساز بدون هیچ فریم‌ورکی.*

</div>

---

## 🧭 این ریپو چیه؟

اکثر پورتفولیوها شبیه هم تولید شدن. این یکی **عمداً طوری ساخته شده که شبیه اونا نباشه**.

`nomoreidea` گوشه شخصی من از وبه: یک صفحه استاتیک و تک‌صفحه‌ای که ایندکس چیزهایی است که
واقعاً مهندسی‌شون کردم — سیستم‌های امبدد، سخت‌افزار دیجیتال، اپلیکیشن‌های دسکتاپ و پلتفرم‌های تحت وب.
نه عکس استوک، نه آمار جعلی، نه «مورد اعتماد بیش از ۵۰,۰۰۰ تیم». هر عددی که توی این سایت می‌بینید واقعیه
و هر اسکرین‌شاتی از یک ریپویی گرفته شده که خودتون می‌تونید بازش کنید.

سیستم طراحی یک تم تیره اختصاصی است که اسمش رو گذاشتم **Cobalt Night**: یک بوم سرمه‌ای عمیق
(`oklch(16% 0.028 258)`) با ریتم لایه‌لایهٔ باندها، خطوط مویی به جای کارت، و دقیقاً یک رنگ سیگنال —
آبی کوبالت الکتریک — که فقط جایی خرج میشه که معنی داشته باشه.
تایپوگرافی هم Space Grotesk + Inter + JetBrains Mono است.

## 🎨 فلسفه طراحی (قوانین Hallmark)

این پروژه از دکترین ضدِ-قالبیِ [Hallmark](https://github.com/nutlope/hallmark) پیروی می‌کنه:

| اصل | نحوه اجرا در این پروژه |
|---|---|
| **صداقت در محتوا** | صفر آمار جعلی. تعداد ریپوها، دروس و نام همکاران همه با GitHub API زنده راستی‌آزمایی شدن. |
| **توکن‌های قفل‌شده** | هر رنگ و فونتی از متغیرهای CSS نام‌دار در `tokens.css` عبور می‌کنه. صفر مقدار hex درون‌خطی در `site.css`. |
| **عدم بازسازی کروم** | نه نوار مرورگر فیک، نه قاب گوشی فیک، نه پنجره IDE فیک. فقط اسکرین‌شات واقعی داخل `<figure>` با حاشیه مویی. |
| **تنوع ساختاری** | صفحه یک *ایندکس* است، نه قالب پیش‌فرض «هیرو → ۳ کارت فیچر → CTA → فوتر». |
| **کف موبایل** | روی ۳۲۰ / ۳۷۵ / ۴۱۴ / ۷۶۸ / ۱۲۸۰ / ۱۹۲۰ پیکسل با آدیتور خودکار CDP تست شده. |
| **دسترسی‌پذیری** | کنتراست WCAG AA برای تمام جفت‌های متن/پس‌زمینه، حلقه فوکوس مرئی، ناوبری کامل با کیبورد، پشتیبانی reduced-motion. |

## ⚡ امکانات (نسخه ۲)

- **ایندکس زندهٔ پروژه‌ها** — بخش نمونه‌کارها مستقیم به گیت‌هاب وصله. یک ریپوی جدید push کنی،
  کارتش خودکار ساخته میشه و خلاصه‌ش از ابتدای README همون ریپو برداشته میشه.
- **چهار پوستهٔ رنگی** — Cobalt Night (پیش‌فرض)، Paper (روشن)، CRT (سبز فسفری)، Ember (گرم).
  با دکمهٔ `◑` یا کلید `T` عوض میشه و در `localStorage` ذخیره میشه.
- **کاملاً دوزبانه** — انگلیسی / فارسی با RTL کامل (کلید `L` یا دکمهٔ EN/فا).
- **کامند پالت** — `Ctrl+K` / `⌘K`: پرش به بخش‌ها، باز کردن ریپوها، تغییر پوسته و زبان.
- **ترمینال تعاملی** — کلید `` ~ ``: فرمان‌های `ls`، `cat <repo>`، `stats`، `theme`، `goto` و…
- **نوار آمار زنده** — تعداد ریپو / ستاره / دنبال‌کننده / سال عضویت، مستقیم از گیت‌هاب.
- **فیلتر و مرتب‌سازی** — جستجو با `/`، چیپ موضوعی، مرتب‌سازی بر اساس تاریخ پوش / ستاره / نام.
- **کیبورد-محور** — نقشهٔ کامل میانبرها زیر `?`، اسکرول-اسپای، نوار پیشرفت،
  انیمیشن‌های ظریف با احترام به reduced-motion.
- **ایستر اگ** — کد Konami رو امتحان کنید.

## 🔄 ایندکس چطور تازه می‌مونه؟

سه لایهٔ مقاوم، بدون هیچ Build Step:

1. **اکشن روزانه** — `.github/workflows/sync-projects.yml` هر شب `tools/sync_projects.py`
   را اجرا می‌کند، `data/projects.json` (لیست ریپوها + خلاصهٔ READMEها) را بازتولید و کامیت می‌کند.
2. **ادغام زنده** — در هر بازدید، `js/data.js` همچنین REST API گیت‌هاب را برای ریپوهای جدیدتر از
   اسنپ‌شات صدا می‌زند و آن‌ها را فوری با پرچم «جدید» نمایش می‌دهد.
3. **خلاصه‌های تنبل + کش** — خلاصهٔ هر کارت فقط وقتی به دید برسد دریافت و در `localStorage`
   کش می‌شود؛ پس خیلی زیر سقف محدودیت نرخ می‌مانیم.

اگر گیت‌هاب در دسترس نباشد، اسنپ‌شاتِ کامیت‌شده رندر می‌شود — سایت هرگز ایندکس خالی نشان نمی‌دهد.

## 🛠️ استک فناوری

عمداً ساده. عمداً سریع.

```
HTML5 · CSS3 (متغیرهای سفارشی، فضای رنگ OKLCH، CSS Grid)
جاوااسکریپت خالص ES2020 · صفر فریم‌ورک · صفر Build Step · صفر ردیاب
گیت‌هاب اکشن برای همگام‌سازی شبانه · گیت‌هاب پیجز برای هاست
```

## 📂 ساختار مخزن

```
nomoreidea/
├── index.html                  # مارکاپ سمانتیک تک‌صفحه‌ای
├── css/
│   ├── tokens.css              # توکن‌های پرتابل OKLCH × ۴ پوسته
│   └── site.css                # لیوت، تایپوگرافی، کامپوننت‌ها، اورلی‌ها
├── js/
│   ├── i18n.js                 # دیکشنری EN/FA + موتور زبان
│   ├── data.js                 # موتور گیت‌هاب → ایندکس پروژه (اسنپ‌شات/زنده/کش)
│   └── app.js                  # رندر، فیلترها، پالت، ترمینال، میانبرها
├── data/
│   └── projects.json           # اسنپ‌شات تولیدشده (روزانه خودکار کامیت میشود)
├── images/
│   ├── mahbod.jpg              # پرتره
│   └── projects/               # اسکرین‌شات‌های واقعی از ریپوهای واقعی
├── tools/
│   ├── audit.py                # آدیتور CDP ریسپانسیو و کنتراست
│   ├── audit-ci.py             # نسخهٔ CI برای ورک‌فلو کیفیت
│   └── sync_projects.py        # بازتولید data/projects.json از API
└── .github/workflows/
    ├── quality.yml             # آدیت دوزبانه/ریسپانسیو روی هر پوش
    └── sync-projects.yml       # همگام‌سازی شبانهٔ ایندکس پروژه‌ها
```

## 🔍 گیت کیفیت خودکار

[`tools/audit.py`](tools/audit.py) رو نوشتم — یک آدیتور headless-Chromium مبتنی بر CDP که صفحه رو
در شش عرض مختلف (۳۲۰ تا ۱۹۲۰ پیکسل) لود می‌کنه و به‌صورت برنامه‌نویسی‌شده بررسی می‌کنه:

- اسکرول افقی در هر ویوپورت
- تارگت‌های لمسی کوچکتر از ۴۴×۴۴ پیکسل (WCAG)
- شکستن متن کلیک‌شونده به دو خط
- خطاهای کنتراست WCAG AA روی هر نود متنی

آخرین اجرا: **TOTAL ISSUES: 0**

```bash
python3 -m http.server 8000
python3 tools/audit.py http://127.0.0.1:8000/index.html
```

## 🚀 نمونه‌کارهای شاخص

پروژه‌هایی که در پورتفولیو ایندکس شدن:

- **[سیستم DAQ پارکینگ هوشمند](https://github.com/Mahbodbe/smart-parking)** — جمع‌آوری داده چندحسی ESP32،
  کنترل PID گیت سروو، احراز هویت دومرحله‌ای ANPR + RFID، داشبورد تله‌متری زنده
- **[پردازنده بسته با Hamming ECC](https://github.com/Mahbodbe/HammingProject)** — موتور RTL به زبان VHDL با
  سریال‌سازی SIPO/PISO، هسته ALU، یونیت کنترل FSM و تصحیح خطای سخت‌افزاری Hamming
- **[Qt Deep Dive](https://github.com/Mahbodbe/Qt-Deep-Dive)** — لاگ مهندسی ۱۴ فصلی Qt/C++ مدرن:
  سینگلتون چندریختی OS، تله‌متری Qt Charts، معماری کتابخانه اشتراکی با لایه DAO
- **[Learning Qt](https://github.com/Mahbodbe/Learning-Qt)** — تمرین‌های پایه GUI: ویجت‌ها،
  سیگنال/اسلات، تم‌های QSS اختصاصی
- **[قفل در هوشمند](https://github.com/Mahbodbe/DoorLock)** — ساخت ماژولار کنترل دسترسی با MFRC522 RFID + PIR +
  بریک‌بین

## 🎓 کارآموزی و پژوهش فعال

در حال حاضر کارآموزی مهندسی در **موج نیرو** رو می‌گذرونم و روی امنیت SCADA، اتوماسیون
پست‌های فشارقوِی متعارف، سیستم‌های DCS و پروتکل‌های IEC 61850 / DNP3 / IEC 104 / Modbus
کار و مطالعه می‌کنم.

## 📄 مجوز

MIT — ببینید [LICENSE](LICENSE). اگر سیستم طراحی رو مفید دیدید آزادانه استفاده کنید؛
یه ⭐ هم حال من رو خوب می‌کنه.

---

<div align="center">

**ساخته [مهبد بمانی‌چم](https://github.com/Mahbodbe)** ·
پلی‌تکنیک تهران · ایران · ۱۴۰۵

</div>
